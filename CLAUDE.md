# قنطار — الواجهة (qintar-frontend)

> **مستودع مستقل.** يستهلك `qintar-backend` عبر HTTP وحده. لا قاعدة بيانات ولا منطق مالي هنا.

## المرجع

الوثائق المرجعية كلها في مستودع **qintar-backend**، وهو مصدر الحقيقة الوحيد:

| ما تحتاجه | أين |
|---|---|
| نماذج البيانات وقواعد العمل ونقاط الـAPI | `qintar-backend/PROJECT_CONTEXT.md` |
| الشاشات الاثنتا عشرة | `PROJECT_CONTEXT.md` القسم ٧ |
| أنواع الموافقات ونصوصها | `qintar-backend/compliance/consent-model.md` |
| ما يُحذف من تقارير الأخطاء | `qintar-backend/compliance/data-map.md` |
| القرارات المحسومة | `qintar-backend/docs/decisions/` |

في التطوير المحلي المستودعان متجاوران، فاقرأ منه مباشرة. **لا تنسخ منه إلى هنا** إلا ما في `shared/`.

## `shared/` — ما يجب أن يتطابق حرفيًا

ثلاثة ملفات وحدها تُنسخ من الباك إند، وعليها حارس بصمة:

- **نصوص الموافقة** — تُعرض حرفيًا، وتُخزَّن بصمتها في `shown_text_hash`. لو تباعد النص عن الباك إند، صار الهاش يوثّق نصًّا غير الذي رآه المستخدم. **هذا خلل قانوني لا تقني.**
- **`categories.json`** — الفئات المعيارية التسع. عقد متطابق مع مشروع ثالث، لا يُعدَّل من طرف واحد.
- **`design-tokens.json`** — مصدر الألوان الوحيد للويب والموبايل.

**تعديل أي منها هنا ممنوع.** يُعدَّل في الباك إند ثم يُنسخ، وتُرفع البصمة.

---

## قواعد غير قابلة للنقض

**١. لا بيانة مالية تغادر التطبيق.** لا حدث تحليلات، لا تقرير خطأ يحمل مبلغًا أو اسم تاجر، لا سجل طرف ثالث. ولا مكتبة تحليلات في المشروع أصلًا.

**٢. لا Hex مباشر في أي مكوّن.** كل لون عبر توكن من `shared/design-tokens.json`. تغيير الهوية لاحقًا تعديل ملف واحد لا بحث واستبدال.

**٣. لا غرض بلا موافقة سارية.** الخمسة مطفأة افتراضيًا: `ai_parsing` · `email_backup` · `error_monitoring` · `card_integration` · `impersonation`. **لا صندوق مؤشَّر مسبقًا، ولا «بمتابعتك فأنت موافق».** وسحب الموافقة بنقرة واحدة بلا سبب مطلوب وبلا شاشة إقناع.

**٤. مراقبة الأخطاء لا تُحمَّل أصلًا قبل الموافقة.** لا تهيئة فارغة — الحزمة لا تُستورد. عدم التحميل أقوى من التهيئة المعطّلة.

**٥. عند إرسال رسالة أو صورة للتحليل: مؤشر مرئي «يُرسل للتحليل…».** لا معالجة صامتة أبدًا.

**٦. لا حفظ عملية بلا مراجعة.** مسارات الإدخال الثلاثة تنتهي كلها بنفس فورم المراجعة قبل الحفظ.

**٧. زر العين إخفاء بصري فقط.** حالة واجهة وتفضيل محلي، ليس تشفيرًا ولا يُوصف كذلك.

---

## قواعد التنفيذ

**الجوال أولًا:** حقول 16px فأكثر · أهداف لمس 44px · `100dvh` لا `100vh` · اختبار على جهاز حقيقي.

**الـAPI تحت `/api/v1/` دائمًا،** وكل استجابة بالشكل `{ success, message, data, errors }`. طبقة العميل تتعامل مع هذا الشكل وحده.

**المصادقة برمز Sanctum** لا كوكيز، لأن الموبايل يشارك نفس الواجهة. الرمز ينتهي بثلاثة أيام خمول.

**المال نصوص لا أعداد.** المبالغ تصل كنص عشري (`"3825.00"`) وتبقى كذلك. `parseFloat` على مبلغ يعيد خطأ الفاصلة العائمة الذي حُذف من الخلفية.

**العمل بلا إنترنت:** الإدخال يعمل ويُزامَن لاحقًا، والمصروف يظهر **«مسودة»** حتى تتم المزامنة، ولا يدخل في أي رقم قبلها.

**العربية افتراضية** واتجاه الصفحة RTL.

---

## توقف واسأل

- تعديل أي ملف في `shared/`
- أي مكتبة تتصل بخدمة خارجية
- أي شاشة تعرض بيانات مالية بطريقة جديدة
- أي شيء يقترب من القواعد السبع أعلاه
